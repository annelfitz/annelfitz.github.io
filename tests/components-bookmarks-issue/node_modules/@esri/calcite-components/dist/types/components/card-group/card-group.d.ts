import { VNode, EventEmitter } from "../../stencil-public-runtime";
import { InteractiveComponent } from "../../utils/interactive";
import { Scale, SelectionMode } from "../interfaces";
import { LoadableComponent } from "../../utils/loadable";
/**
 * @slot - A slot for adding one or more `calcite-card`s.
 */
export declare class CardGroup implements InteractiveComponent, LoadableComponent {
    el: HTMLCalciteCardGroupElement;
    /** When `true`, interaction is prevented and the component is displayed with lower opacity. */
    disabled: boolean;
    /** Accessible name for the component. */
    label: string;
    /** Specifies the size of the component. Child `calcite-card`s inherit the component's value. */
    scale: Scale;
    /** Specifies the selection mode of the component. */
    selectionMode: Extract<"multiple" | "single" | "single-persist" | "none", SelectionMode>;
    onSelectionModeChange(): void;
    /**
     * Specifies the component's selected items.
     *
     * @readonly
     */
    selectedItems: HTMLCalciteCardElement[];
    private items;
    private slotRefEl;
    /** Emits when the component's selection changes and the `selectionMode` is not `none`. */
    calciteCardGroupSelect: EventEmitter<void>;
    connectedCallback(): void;
    componentDidRender(): void;
    componentDidLoad(): void;
    disconnectedCallback(): void;
    componentWillLoad(): Promise<void>;
    calciteInternalCardKeyEventListener(event: KeyboardEvent): void;
    calciteCardSelectListener(event: CustomEvent): void;
    /**
     * Sets focus on the component's first focusable element.
     */
    setFocus(): Promise<void>;
    private udpateItemsOnSelectionModeChange;
    private updateItemsOnSlotChange;
    private updateSlottedItems;
    private updateSelectedItems;
    private setSelectedItems;
    render(): VNode;
}
