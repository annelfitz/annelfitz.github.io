import { VNode } from "../../stencil-public-runtime";
import { LoadableComponent } from "../../utils/loadable";
import { T9nComponent } from "../../utils/t9n";
import { LocalizedComponent } from "../../utils/locale";
import { Alignment, Scale, SelectionMode } from "../interfaces";
import { TableHeaderMessages } from "./assets/table-header/t9n";
import { RowType, TableInteractionMode } from "../table/interfaces";
export declare class TableHeader implements LocalizedComponent, LoadableComponent, T9nComponent {
    /** Specifies the alignment of the component. */
    alignment: Alignment;
    /** Specifies the number of columns the component should span. */
    colSpan: number;
    /** A description to display beneath heading content. */
    description: string;
    /** A heading to display above description content. */
    heading: string;
    /** Specifies the number of rows the component should span. */
    rowSpan: number;
    /** @internal */
    interactionMode: TableInteractionMode;
    /** @internal */
    lastCell: boolean;
    /** @internal */
    numberCell: boolean;
    /** @internal */
    parentRowIsSelected: boolean;
    /** @internal */
    parentRowType: RowType;
    /** @internal */
    positionInRow: number;
    /** @internal */
    scale: Scale;
    /** @internal */
    selectedRowCount: number;
    /** @internal */
    selectedRowCountLocalized: string;
    onSelectedChange(): void;
    /** @internal */
    selectionCell: boolean;
    /** @internal */
    selectionMode: SelectionMode;
    /** @internal */
    bodyRowCount: number;
    /**
     * Made into a prop for testing purposes only
     *
     * @internal
     */
    messages: TableHeaderMessages;
    /**
     * Use this property to override individual strings used by the component.
     */
    messageOverrides: Partial<TableHeaderMessages>;
    onMessagesChange(): void;
    componentWillLoad(): Promise<void>;
    componentDidLoad(): void;
    connectedCallback(): void;
    disconnectedCallback(): void;
    el: HTMLCalciteTableHeaderElement;
    defaultMessages: TableHeaderMessages;
    focused: boolean;
    screenReaderText: string;
    effectiveLocale: string;
    effectiveLocaleChange(): void;
    private containerEl;
    /** Sets focus on the component. */
    setFocus(): Promise<void>;
    private updateScreenReaderText;
    private onContainerBlur;
    private onContainerFocus;
    render(): VNode;
}
