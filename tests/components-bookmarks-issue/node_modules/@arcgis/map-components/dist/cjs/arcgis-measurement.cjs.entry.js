/*!
 * All material copyright Esri, All Rights Reserved, unless otherwise specified.
 * See https://js.arcgis.com/4.30/esri/copyright.txt for details.
 * v4.30.0-next.13
 */
'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const index = require('./index-6e62126d.js');
const index$1 = require('./index-b2323407.js');
const componentUtils = require('./component-utils-e47db275.js');
const disconnected = require('./disconnected-bc6a8942.js');

const measurementCss = "arcgis-measurement{}";

const ArcgisMeasurement = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        this.arcgisMeasurementReady = index.createEvent(this, "arcgisMeasurementReady", 7);
        this._watchHandles = [];
        this.referenceElement = undefined;
        this.activeTool = undefined;
        this.activeComponent = undefined;
        this.areaUnit = undefined;
        this.icon = undefined;
        this.linearUnit = undefined;
        this.position = "bottom-left";
        this.view = undefined;
        this.widget = undefined;
    }
    referenceElementWatcher(value) {
        void componentUtils.referenceElementHelper(this, value);
    }
    activeToolWatcher(value) {
        if (this.widget) {
            this.widget.activeTool = value;
        }
    }
    areaUnitWatcher(value) {
        if (this.widget) {
            this.widget.areaUnit = value;
        }
    }
    iconWatcher(value) {
        if (this.widget) {
            this.widget.icon = value;
        }
    }
    linearUnitWatcher(value) {
        if (this.widget) {
            this.widget.linearUnit = value;
        }
    }
    positionWatcher(value) {
        if (value !== undefined && this.widget) {
            this.view.ui.move(this.childElem, value);
        }
    }
    viewWatcher(value) {
        if (value !== undefined && this.widget) {
            this.widget.view = value;
        }
    }
    //--------------------------------------------------------------------------
    //
    //  Methods
    //
    //--------------------------------------------------------------------------
    async clear() {
        this.widget?.clear();
    }
    async startMeasurement() {
        this.widget?.startMeasurement();
    }
    //--------------------------------------------------------------------------
    //
    //  Lifecycle
    //
    //--------------------------------------------------------------------------
    async componentDidLoad() {
        const params = this._createParams();
        const widget = await index$1.newWidgetsMeasurement(params);
        this.widget = widget;
        await this._addWatchers();
        this._addContainer();
        this.arcgisMeasurementReady.emit();
        componentUtils.checkForView(this);
    }
    disconnectedCallback() {
        disconnected.onDisconnectedHelper(this);
    }
    render() {
        return index.h("div", { ref: (el) => (this.childElem = el) });
    }
    //--------------------------------------------------------------------------
    //
    //  Private Methods
    //
    //--------------------------------------------------------------------------
    _addContainer() {
        this.widget.container = this.childElem;
    }
    async _addWatchers() {
        const { watch } = await index$1.importCoreReactiveUtils();
        componentUtils.addHandles(this, watch(() => this.widget.activeWidget, (value) => (this.activeComponent = value), {
            initial: true
        }));
    }
    _createParams() {
        const params = {};
        if (this.activeTool) {
            params.activeTool = this.activeTool;
        }
        if (this.areaUnit) {
            params.areaUnit = this.areaUnit;
        }
        if (this.linearUnit) {
            params.linearUnit = this.linearUnit;
        }
        if (this.view) {
            params.view = this.view;
        }
        return params;
    }
    get el() { return index.getElement(this); }
    static get watchers() { return {
        "referenceElement": ["referenceElementWatcher"],
        "activeTool": ["activeToolWatcher"],
        "areaUnit": ["areaUnitWatcher"],
        "icon": ["iconWatcher"],
        "linearUnit": ["linearUnitWatcher"],
        "position": ["positionWatcher"],
        "view": ["viewWatcher"]
    }; }
};
ArcgisMeasurement.style = measurementCss;

exports.arcgis_measurement = ArcgisMeasurement;
