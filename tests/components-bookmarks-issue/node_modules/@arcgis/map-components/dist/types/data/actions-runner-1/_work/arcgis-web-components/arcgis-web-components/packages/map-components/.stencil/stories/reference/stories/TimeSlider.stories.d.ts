import type { Meta, StoryObj } from "@storybook/web-components";
import type { TimeSliderWidget } from "../../../src/components";
import type { StoryProps } from "@arcgis/storybook-utils";
interface TimeSliderProps extends StoryProps {
    fullTimeExtent: TimeSliderWidget["fullTimeExtent"];
    layout: "auto" | "compact" | "wide";
    position: "bottom-left" | "bottom-right" | "manual" | "top-left" | "top-right";
    loop: boolean;
    stops: TimeSliderWidget["stops"];
    mode: "cumulative-from-end" | "cumulative-from-start" | "instant" | "time-window";
    playRate: number;
    timeVisible: boolean;
    timeZone: string;
}
declare const meta: Meta<TimeSliderProps>;
export default meta;
export declare const TimeSlider: StoryObj<TimeSliderProps>;
