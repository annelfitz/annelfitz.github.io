/**
 * This story was created to quickly test the map component
 * with simple widgets. We can later expand this story to conditionally offer
 * more controls based on the selected widgets.
 */
import type { Meta, StoryObj } from "@storybook/web-components";
interface MapProps {
    mapItemId: string;
    topLeftWidget: string;
    topRightWidget: string;
    bottomLeftWidget: string;
    bottomRightWidget: string;
}
type Story = StoryObj<MapProps>;
declare const meta: Meta<MapProps>;
export default meta;
export declare const Map: Story;
