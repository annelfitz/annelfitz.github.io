import {
  i
} from "./chunk-2WUPZJUL.js";
import {
  f4 as f,
  n as n2
} from "./chunk-4IJED6JM.js";
import {
  c,
  d
} from "./chunk-C5EJV3NW.js";
import {
  x
} from "./chunk-IJD65EMQ.js";
import {
  e2 as e,
  m
} from "./chunk-JTUYA5FE.js";
import {
  B,
  C,
  E,
  H,
  M,
  q,
  y,
  z
} from "./chunk-AVR3LWTZ.js";
import {
  D,
  L
} from "./chunk-WKVHVZW2.js";
import {
  n2 as n,
  s
} from "./chunk-7RBRCL6S.js";
import {
  has
} from "./chunk-J2EGIWT6.js";

// node_modules/@arcgis/core/views/2d/engine/webgl/AttributeStoreView.js
var b = () => n.getLogger("esri.views.2d.engine.webgl.AttributeStoreView");
var T = class {
  constructor(t, e2, i3) {
    this._texture = null, this._lastTexture = null, this._fbos = {}, this.texelSize = 4;
    const { buffer: s2, pixelType: r2, textureOnly: a } = t, n3 = d(r2);
    this.blockIndex = i3, this.pixelType = r2, this.size = e2, this.textureOnly = a, a || (this.data = new n3(s2)), this._resetRange();
  }
  destroy() {
    var _a;
    (_a = this._texture) == null ? void 0 : _a.dispose();
    for (const t in this._fbos) {
      const e2 = this._fbos[t];
      e2 && ("0" === t && e2.detachColorTexture(), e2.dispose()), this._fbos[t] = null;
    }
    this._texture = null;
  }
  get _textureDesc() {
    const t = new e();
    return t.wrapMode = D.CLAMP_TO_EDGE, t.samplingMode = L.NEAREST, t.dataType = this.pixelType, t.width = this.size, t.height = this.size, t;
  }
  setData(t, e2, i3) {
    const s2 = f(t), r2 = this.data, a = s2 * this.texelSize + e2;
    !r2 || a >= r2.length || (r2[a] = i3, this.dirtyStart = Math.min(this.dirtyStart, s2), this.dirtyEnd = Math.max(this.dirtyEnd, s2));
  }
  getData(t, e2) {
    if (null == this.data)
      return null;
    const i3 = f(t) * this.texelSize + e2;
    return !this.data || i3 >= this.data.length ? null : this.data[i3];
  }
  getTexture(t) {
    return this._texture ?? this._initTexture(t);
  }
  getFBO(t, e2 = 0) {
    if (!this._fbos[e2]) {
      const i3 = 0 === e2 ? this.getTexture(t) : this._textureDesc;
      this._fbos[e2] = new x(t, i3);
    }
    return this._fbos[e2];
  }
  get hasDirty() {
    const t = this.dirtyStart;
    return this.dirtyEnd >= t;
  }
  updateTexture(e2, i3) {
    try {
      const s2 = this.dirtyStart, r2 = this.dirtyEnd;
      if (!this.hasDirty)
        return;
      has("esri-2d-update-debug") && console.debug(`Version[${i3}] AttributeStoreView.updateTexture`, { start: s2, end: r2, firstBytes: new Uint8Array(this.data.buffer.slice(0, 16)), block: this }), this._resetRange();
      const a = this.data.buffer, n3 = this.getTexture(e2), u = 4, h = (s2 - s2 % this.size) / this.size, o = (r2 - r2 % this.size) / this.size, d2 = h, p = this.size, _ = o, g = h * this.size * u, c2 = (p + _ * this.size) * u - g, x2 = d(this.pixelType), f2 = new x2(a, g * x2.BYTES_PER_ELEMENT, c2), T2 = this.size, m3 = _ - d2 + 1;
      if (m3 > this.size)
        return void b().error(new s("mapview-webgl", "Out-of-bounds index when updating AttributeData"));
      n3.updateData(0, 0, d2, T2, m3, f2);
    } catch (s2) {
    }
  }
  update(t) {
    const { data: e2, start: i3, end: s2 } = t;
    if (null != e2 && null != this.data) {
      const s3 = this.data, r2 = i3 * this.texelSize;
      for (let i4 = 0; i4 < e2.length; i4++) {
        const a = 1 << i4 % this.texelSize;
        t.layout & a && (s3[r2 + i4] = e2[i4]);
      }
    }
    this.dirtyStart = Math.min(this.dirtyStart, i3), this.dirtyEnd = Math.max(this.dirtyEnd, s2);
  }
  resize(t, e2) {
    const i3 = this.size;
    if (this.size = e2, this.textureOnly)
      return void (i3 !== this.size && (this._lastTexture = this._texture, this._texture = null));
    const s2 = d(this.pixelType);
    this.destroy(), this.data = new s2(t.buffer);
  }
  _resetRange() {
    this.dirtyStart = 2147483647, this.dirtyEnd = 0;
  }
  _initTexture(t) {
    const e2 = new m(t, this._textureDesc, this.data ?? void 0);
    if (null != this._lastTexture && this._fbos[0]) {
      const i3 = this._lastTexture.descriptor.width, s2 = this._lastTexture.descriptor.height, r2 = this._lastTexture.descriptor.dataType, a = this._lastTexture.descriptor.pixelFormat, n3 = this.getFBO(t), u = c(r2), h = new (d(r2))(new ArrayBuffer(i3 * s2 * u * this.texelSize)), o = t.getBoundFramebufferObject(), { x: d2, y: _, width: g, height: c2 } = t.getViewport();
      t.bindFramebuffer(n3), n3.readPixels(0, 0, i3, s2, a, r2, h), e2.updateData(0, 0, 0, 2 * i3, s2 / 2, h), t.setViewport(d2, _, g, c2), t.bindFramebuffer(o);
    }
    return this.destroy(), this._texture = e2, this._texture;
  }
};
var m2 = class {
  constructor() {
    this.size = 0, this._pendingAttributeUpdates = [], this._version = 0, this._epoch = 0, this._locked = false;
  }
  _initialize(t) {
    if (!t)
      throw new Error("InternalError: initArgs must be defined");
    const e2 = t.blockDescriptors;
    if (this.size = t.blockSize, has("esri-2d-update-debug") && console.debug("AttributeStoreView.initialize", { message: t }), null == this._data)
      this._data = e2.map((t2, e3) => null != t2 ? new T(t2, this.size, e3) : null);
    else
      for (let i3 = 0; i3 < this._data.length; i3++) {
        const t2 = this._data[i3], s2 = e2[i3];
        null != s2 && (null == t2 ? this._data[i3] = new T(s2, this.size, i3) : t2.resize(s2, this.size));
      }
  }
  destroy() {
    var _a;
    for (const t of this._data ?? [])
      t == null ? void 0 : t.destroy();
    (_a = this._defaultTexture) == null ? void 0 : _a.dispose(), this._pendingAttributeUpdates = [];
  }
  isEmpty() {
    return null == this._data;
  }
  getBlock(t) {
    if (null == this._data)
      return null;
    return this._data[t];
  }
  setLabelMinZoom(t, e2) {
    this.setData(t, 0, 1, e2);
  }
  getLabelMinZoom(t) {
    return this.getData(t, 0, 1, 255);
  }
  getFilterFlags(t) {
    return this.getData(t, 0, 0, 0);
  }
  getVVSize(t) {
    return this.getData(t, M.VV, 0, 0);
  }
  getData(t, e2, i3, s2) {
    if (!this._data)
      return 0;
    const r2 = this._data[e2];
    if (null == r2)
      return 0;
    const a = r2.getData(t, i3);
    return null != a ? a : s2;
  }
  setData(t, e2, i3, s2) {
    this._data[e2].setData(t, i3, s2);
  }
  lockTextureUploads() {
    this._locked = true;
  }
  unlockTextureUploads() {
    this._locked = false, this.update();
  }
  requestUpdate(t) {
    this._version = t.version, this._pendingAttributeUpdates.push(t), has("esri-2d-update-debug") && console.debug(`Version[${this._version}] AttributeStoreView.requestUpdate`, { message: t });
  }
  get currentEpoch() {
    return this._epoch;
  }
  update() {
    if (this._locked)
      return;
    const t = this._pendingAttributeUpdates;
    this._pendingAttributeUpdates = [];
    for (const e2 of t) {
      const { blockData: t2, initArgs: i3, sendUpdateEpoch: s2, version: r2 } = e2;
      has("esri-2d-update-debug") && console.debug(`Version[${this._version}] Epoch[${s2}] AttributeStoreView.applyUpdate`), this._version = r2, this._epoch = s2, null != i3 && this._initialize(i3);
      const a = this._data;
      for (let e3 = 0; e3 < t2.length; e3++) {
        const i4 = t2[e3], s3 = a[e3];
        null != s3 && null != i4 && (has("esri-2d-update-debug") && console.debug(`Version[${this._version}] CpuBlock[${e3}] AttributeStoreView.update`, { block: i4 }), s3.update(i4));
      }
    }
  }
  getUniforms(t) {
    return { filterFlags: { texture: this._getTexture(t, M.FilterFlags), unit: q }, animation: { texture: this._getTexture(t, M.Animation), unit: y }, gpgpu: { texture: this._getTexture(t, M.GPGPU), unit: H }, visualVariableData: { texture: this._getTexture(t, M.VV), unit: z }, dataDriven0: { texture: this._getTexture(t, M.DD0), unit: B }, dataDriven1: { texture: this._getTexture(t, M.DD1), unit: C }, dataDriven2: { texture: this._getTexture(t, M.DD2), unit: E }, size: this.size };
  }
  _getTexture(t, e2) {
    var _a;
    const i3 = (_a = this._data) == null ? void 0 : _a[e2];
    return i3 ? (i3.updateTexture(t, this._version), i3.getTexture(t)) : this._getDefaultTexture(t);
  }
  _getDefaultTexture(t) {
    if (null == this._defaultTexture) {
      const e2 = new e();
      e2.wrapMode = D.CLAMP_TO_EDGE, e2.samplingMode = L.NEAREST, e2.width = 1, e2.height = 1, this._defaultTexture = new m(t, e2, new Uint8Array(4));
    }
    return this._defaultTexture;
  }
};

// node_modules/@arcgis/core/views/2d/engine/AFeatureContainer.js
var r = class extends i {
  constructor(t) {
    super(t), this._statisticsByLevel = /* @__PURE__ */ new Map(), this.attributeView = new m2();
  }
  destroy() {
    this.children.forEach((e2) => e2.destroy()), this.removeAllChildren(), this.attributeView.destroy();
  }
  doRender(e2) {
    e2.context.capabilities.enable("textureFloat"), super.doRender(e2);
  }
  createRenderParams(e2) {
    const t = super.createRenderParams(e2);
    return t.attributeView = this.attributeView, t.instanceStore = this._instanceStore, t.statisticsByLevel = this._statisticsByLevel, t;
  }
};

// node_modules/@arcgis/core/views/2d/engine/webgl/shaderGraph/techniques/TechniqueInstance.js
var i2 = class {
  constructor(t, i3, e2, s2, n3) {
    this._instanceId = t, this.techniqueRef = i3, this._meshWriterName = e2, this._input = s2, this.optionalAttributes = n3;
  }
  get instanceId() {
    return n2(this._instanceId);
  }
  createMeshInfo(t) {
    return { id: this._instanceId, meshWriterName: this._meshWriterName, options: t, optionalAttributes: this.optionalAttributes };
  }
  getInput() {
    return this._input;
  }
  setInput(t) {
    this._input = t;
  }
};

export {
  r,
  i2 as i
};
//# sourceMappingURL=chunk-SGCIDT4C.js.map
